Table dim_year {
  year_id int [pk]
  year int [not null]
}

Table dim_municipality {
  municipality_id varchar(32) [pk]
  municipality_name varchar [not null]
}

Table fact_municipality_overview {
  year int
  year_id int [not null]
  municipality_id varchar(32) [not null]
  population int
  deaths int
  death_rate_per_1000 decimal(10,2)
  establishments_count int
  personnel_staff_years int
  total_bankruptcies_enterprises int
  total_bankruptcies_employees int
  bankruptcies_per_1000_establishments decimal(10,2)
  bankruptcies_per_100k_population decimal(10,2)
}

Ref: fact_municipality_overview.year_id > dim_year.year_id
Ref: fact_municipality_overview.municipality_id > dim_municipality.municipality_id

Index fact_municipality_overview_grain {
  fact_municipality_overview.year_id
  fact_municipality_overview.municipality_id
  [unique]
}